version: '3.10'

services:
  django:
    command: bash -s "python manage.py makemigrations && python manage.py migrate && python manage.py runserver"
    image: django-docker:0.0.1
    build: .
    ports:
      - "8000:8000"